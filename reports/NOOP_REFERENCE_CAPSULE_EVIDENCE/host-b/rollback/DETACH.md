# Detach and Rollback

Detach by removing the host-local module/package reference, adapter, and integration manifest. Clear only capsule-owned transient state in namespace `swrlz.reference.noop`. Preserve canonical ZIP/SHA, lineage records, test reports, and unrelated host data.
