# Descriptor Evidence

The descriptor declares Kotlin/JVM runtime support, audit and clock as required services, ephemeral state as optional, zero permissions, zero components, zero network routes, isolated storage namespace `swrlz.reference.noop`, optional-isolated failure policy, and audit-only Truth Firewall impact. Descriptor identity and service requirements are also represented by typed Kotlin contracts and validated by deterministic tests.
