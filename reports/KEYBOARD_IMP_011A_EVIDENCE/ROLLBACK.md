# KEYBOARD-IMP-011A Rollback

This checkpoint is additive except for the Keyboard lane README classification.

To roll back before merge:

1. leave `checkpoint/keyboard-imp-011a` unmerged;
2. return to the accepted predecessor branch;
3. preserve this branch and evidence for audit rather than deleting it.

If later promoted and superseded:

1. preserve `SWRLZ_KEYBOARD_IME_CFv0.1.0_SOURCE.zip` and its checksum under an explicit lineage location;
2. restore the last accepted Keyboard source selection and README state;
3. do not reactivate the historical Core-shaped seed as a Keyboard implementation without an explicit decision;
4. retain package identity, signer, version, and failure evidence.

No existing seed archive was changed, so predecessor recovery remains available.
