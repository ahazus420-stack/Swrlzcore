# 2026-07-22 — INT-BUILD-020A

## Objective

Recover the CLIENT CFv2.0.8 build and the SERVER CFv2.0.9 source-resolution workflow without changing runtime architecture.

## Source lineage

- CLIENT CFv2.0.8 → CFv2.0.9
- SERVER CFv2.0.9 → CFv2.0.10

## Facts

- CLIENT reached Kotlin compilation and failed on four `truth.auto.connected` references.
- `CoreNodeCheckRecord` exposes `checked`, `success`, `summary`, `detail`, and `checkedAt`; it does not expose `connected`.
- SERVER stopped during source resolution because no matching checksum companion was found for the selected ZIP.

## Engineering decisions

- Use the existing authoritative check-result property `success`; do not introduce a redundant `connected` property.
- Issue a new SERVER version with an exact same-basename ZIP/checksum pair rather than ambiguously reusing CFv2.0.9.
- Preserve all registration, presence, trust, identity, lifecycle, Room, and protocol behavior.

## Engineering changes

- Replaced four CLIENT automatic-state references with `truth.auto.success`.
- Advanced CLIENT to versionCode 40 / CFv2.0.9.
- Advanced SERVER to versionCode 13 / CFv2.0.10 for canonical package resolution.
- Added package-local repair/source-resolution notes.

## Verification performed

- CLIENT invalid-reference absence guard: PASS.
- CLIENT canonical success-reference guard: PASS.
- CLIENT and SERVER version guards: PASS.
- ZIP integrity: PASS for both packages.
- Exact-name SHA-256 companions generated.

## Evidence status

- SOURCE IMPLEMENTED
- STATIC VERIFICATION PASS
- BUILD NOT RUN
- RUNTIME NOT TESTED
- WORKFLOW NOT TRIGGERED
- REPOSITORY DOCUMENTATION PUBLISHED

## Follow-up

Upload each source ZIP together with its exact `.sha256` file, run both workflows, and return the build evidence.