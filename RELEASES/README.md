# SWRLZ Releases

This folder holds milestone build outputs when the APK forge workflow is run with commit_release_artifacts enabled.

For normal testing, prefer the downloadable GitHub Actions artifact so the repo stays smaller.

Recommended structure:

```text
RELEASES/<release>_APK/
  APK download zip
  debug APK
  SHA256 files
  build provenance report
  GitHub Actions build summary
```
