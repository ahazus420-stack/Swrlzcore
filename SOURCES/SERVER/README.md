# SERVER Source Lane

Place the current active server source set here.

## Expected naming

- SERVER_CFvX.Y.Z_SWRLZ.zip
- SERVER_CFvX.Y.Z_SWRLZ.sha256
- SERVER_CFvX.Y.Z_SWRLZ.md

## Hygiene rule

Keep only the active server source set at the top of this lane. Archive older outputs into:

- SOURCES/SERVER/OLD_PATCHES/

Do not delete by default. Preserve lineage.
