# Packages

The canonical package for this checkpoint is stored at the capsule-lane root:

- `../SWRLZ_DISCOVERY_CONTRACT_CAPSULE_v0.1.0.zip`
- `../SWRLZ_DISCOVERY_CONTRACT_CAPSULE_v0.1.0.sha256`

This directory is reserved for future package indexing without duplicating the canonical artifact.

The ZIP contains the standalone Gradle source project: descriptor, public API, internal backends, tests, and canonical vectors. Lane-level implementation notes and evidence remain sibling repository records rather than duplicated inside the source package.
