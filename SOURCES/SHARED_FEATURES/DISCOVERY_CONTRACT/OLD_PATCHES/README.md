# OLD_PATCHES

No predecessor capsule exists for `swrlz.discovery.contract` version `0.1.0`.
Future superseded package versions must be preserved here with lineage and checksums rather than silently deleted.
