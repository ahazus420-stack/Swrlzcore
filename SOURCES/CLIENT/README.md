# CLIENT Source Lane

Place the current active client source set here.

## Expected naming

- CLIENT_CFvX.Y.Z_SWRLZ.zip
- CLIENT_CFvX.Y.Z_SWRLZ.sha256
- CLIENT_CFvX.Y.Z_SWRLZ.md

## Hygiene rule

Keep only the active client source set at the top of this lane. Archive older outputs into:

- SOURCES/CLIENT/OLD_PATCHES/

Do not delete by default. Preserve lineage.
